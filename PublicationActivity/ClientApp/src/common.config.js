const config = {
    supportedLanguages: [{ identifier: 'en' }, { identifier: 'bg', default: true }],
    uiColors: {
        success: '#d4ffdd',
        error: '#ffdee6',
        warning: '#faf8cd'
    }
};

module.exports = config;
